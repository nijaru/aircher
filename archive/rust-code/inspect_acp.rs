use agent_client_protocol::*;

fn main() {
    // Let's inspect the PromptRequest structure
    println!("Inspecting ACP types...");

    // Try to create a PromptRequest to see its fields
    let prompt_request = PromptRequest {
        // We'll see what fields are available
    };
}
